<h1>Oden Simulator</h1>
<h3>Where you can enjoy this Japanese wintertime delicacy -- anywhere, any season.</h3>

<div class="container">
    <div class="row">
        <div class="col-4">
            <h4>Ingredient Types</h4>
            <ul>
                {{#each types}}
                <li>{{name}} <button type="button" class="type-btn btn btn-primary btn-sm" data-type="{{id}}"><i class="fas fa-sign-in-alt"></i></button></li>
                {{/each}}
            </ul>
        </div>
        <div class="col-4">
            <h4>Ready to Eat</h4>
            <ul>
                {{#each ingredients}}
                {{#unless devoured}}
                <li>{{name}}</li>
                {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class="col-4">
            <h4>Already Eaten</h4>
            <ul>
                {{#each ingredients}}
                {{#if devoured}}
                <li>{{name}}</li>
                {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>
</div>